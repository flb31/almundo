{
  "name": "almundo.com",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "server": "nodemon --exec babel-node server.js",
    "test": "mocha \"./api/test/**/*.spec.js*\"",
    "app": "cd app && yarn start"
  },
  "nodemonConfig": {
    "ignore": [
      "app/*"
    ]
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "mocha": "^5.2.0"
  }
}
